apiVersion: v1
kind: Service
metadata:
  name: api-server
  labels:
    app: api-server
    app.kubernetes.io/name: adult-platform-trust-safety
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: api-server
