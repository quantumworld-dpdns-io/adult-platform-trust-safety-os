apiVersion: v1
kind: Service
metadata:
  name: {{ include "adult-platform-trust-safety.fullname" . }}
  labels:
    {{- include "adult-platform-trust-safety.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adult-platform-trust-safety.selectorLabels" . | nindent 4 }}
